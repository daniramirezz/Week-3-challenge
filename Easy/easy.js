var person1 = "Justin"; 
var person2 = "Dylan";

if(person1.length > person2.length){
    console.log(`${person1}'s name is longer than ${person2}'s name by ${person1.length - person2.length} character's`)
}
